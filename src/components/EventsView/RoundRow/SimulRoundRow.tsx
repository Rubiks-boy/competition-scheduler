import { MainSimulRow } from "./MainSimulRow";
import type { RoundRowProps } from "./types";

export const SimulRoundRow = (props: RoundRowProps) => {
  return (
    <>
      <MainSimulRow {...props} />
    </>
  );
};
